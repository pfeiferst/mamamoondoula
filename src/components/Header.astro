---
import { SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
---

<header>
	<nav>
		<h2><a href="/">{SITE_TITLE}</a></h2>
		<div class="internal-links">
			<HeaderLink href="/">Home</HeaderLink>
			<HeaderLink href="/about">Über mich</HeaderLink>
			<HeaderLink href="/schwangeren-massage">Schwangeren-Massage</HeaderLink>
		</div>
		<div class="contact-info">
			<a href="https://calendly.com/your-calendly-link" class="calendly-link" target="_blank" rel="noopener noreferrer">
				<span class="calendly-icon">📅</span>
				<span class="calendly-text">Termin buchen</span>
			</a>
			<a href="tel:0170-5580548" class="phone-link">
				<span class="phone-icon">📞</span>
				<span class="phone-number">0170-5580548</span>
			</a>
		</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}
	nav a {
		padding: 1em 0.5em;
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}
	.contact-info {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		margin-left: 2rem;
		padding: 0.5rem 0;
	}
	
	.phone-link {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		background: linear-gradient(135deg, #f0b7b3, #dd7e8d);
		color: #1c1c1c;
		padding: 1rem 1.5rem;
		border-radius: 30px;
		text-decoration: none;
		font-weight: 600;
		font-size: 0.95rem;
		box-shadow: 0 2px 8px rgba(240, 183, 179, 0.3);
		transition: all 0.3s ease;
		border: 2px solid transparent;
		margin: 0.5rem;
	}
	
	.phone-link:hover {
		background: linear-gradient(135deg, #dd7e8d, #f0b7b3);
		box-shadow: 0 4px 12px rgba(240, 183, 179, 0.4);
		transform: translateY(-1px);
		border-color: rgba(221, 126, 141, 0.3);
	}
	
	.phone-icon {
		font-size: 1.1rem;
		opacity: 0.9;
	}
	
	.phone-number {
		font-family: 'Quicksand', sans-serif;
		letter-spacing: 0.5px;
	}
	
	.calendly-link {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		background: linear-gradient(135deg, #4a90e2, #357abd);
		color: white;
		padding: 1rem 1.5rem;
		border-radius: 30px;
		text-decoration: none;
		font-weight: 600;
		font-size: 0.95rem;
		box-shadow: 0 2px 8px rgba(74, 144, 226, 0.3);
		transition: all 0.3s ease;
		border: 2px solid transparent;
		margin: 0.5rem;
	}
	
	.calendly-link:hover {
		background: linear-gradient(135deg, #357abd, #4a90e2);
		box-shadow: 0 4px 12px rgba(74, 144, 226, 0.4);
		transform: translateY(-1px);
		border-color: rgba(53, 122, 189, 0.3);
	}
	
	.calendly-icon {
		font-size: 1.1rem;
		opacity: 0.9;
	}
	
	.calendly-text {
		font-family: 'Quicksand', sans-serif;
		letter-spacing: 0.5px;
	}
	
	@media (max-width: 720px) {
		.contact-info {
			margin-left: 1rem;
		}
		
		.calendly-link {
			padding: 0.75rem 1rem;
			font-size: 0.85rem;
			margin: 0.25rem;
		}
		
		.calendly-text {
			display: none;
		}
		
		.calendly-icon {
			font-size: 1.2rem;
		}
		
		.phone-link {
			padding: 0.75rem 1rem;
			font-size: 0.85rem;
			margin: 0.25rem;
		}
		
		.phone-number {
			display: none;
		}
		
		.phone-icon {
			font-size: 1.2rem;
		}
	}
	
	@media (max-width: 480px) {
		.contact-info {
			display: none;
		}
	}
</style>
